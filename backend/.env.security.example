# ============================================================================
# API Authentication Keys (Gemini Suggestion #1)
# ============================================================================
# Copy this to .env and set your own secret keys
#
# Security Best Practices:
# 1. Use strong, random keys (min 32 characters)
# 2. Never commit .env file to git
# 3. Rotate keys regularly (every 3-6 months)
# 4. Use different keys for different environments (dev/staging/prod)
#
# Key Hierarchy:
# - MASTER: Full access (use sparingly, admin only)
# - TRADING: Execute trades, approve signals
# - WEBHOOK: External services (Telegram, Slack)
# - READONLY: View data, monitoring (safest for dashboards)
# ============================================================================

# Master API Key - FULL ACCESS (admin only!)
# Generate with: openssl rand -hex 32
API_MASTER_KEY=your-super-secret-master-key-min-32-chars

# Trading API Key - Can execute trades and approve signals
API_TRADING_KEY=your-trading-api-key-min-32-chars

# Readonly API Key - View data only (safe for monitoring dashboards)
API_READONLY_KEY=your-readonly-api-key-min-32-chars

# Webhook API Key - For external services (Telegram bot, Slack webhook)
API_WEBHOOK_KEY=your-webhook-api-key-min-32-chars

# Development Mode - DISABLE authentication (NEVER use in production!)
# Set to "true" to disable auth (local testing only)
API_AUTH_DISABLED=false

# Rate Limiting - requests per minute per API key
API_RATE_LIMIT=60

# ============================================================================
# Existing Environment Variables (keep these)
# ============================================================================

# AI APIs
CLAUDE_API_KEY=your-claude-api-key
GEMINI_API_KEY=your-gemini-api-key
OPENAI_API_KEY=your-openai-api-key

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/trading

# Redis Cache
REDIS_HOST=localhost
REDIS_PORT=6379

# Telegram Bot (for notifications)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id

# Slack Webhook (for notifications)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ============================================================================
# How to Generate Secure Keys
# ============================================================================
#
# Option 1 (Linux/Mac):
#   openssl rand -hex 32
#
# Option 2 (Python):
#   python -c "import secrets; print(secrets.token_hex(32))"
#
# Option 3 (Node.js):
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Option 4 (Online - use with caution):
#   https://randomkeygen.com/ (CodeIgniter Encryption Keys section)
#
# ============================================================================
