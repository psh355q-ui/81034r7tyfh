---
name: ultra-thin-orchestrate
description: 대규모 프로젝트(50-200개 태스크)를 위한 초슬림 오케스트레이션 모드. 컨텍스트를 76% 절감하고, 메인 에이전트는 "교통정리"만 담당하며 서브에이전트가 실제 작업을 수행합니다. "/orchestrate --ultra-thin", "/auto-orchestrate --ultra-thin", "울트라씬 모드로 실행" 등의 키워드에 반응합니다.
---

# Ultra-Thin Orchestrate Skill

## 핵심 원리

```
┌─────────────────────────────────────────────────────────────────┐
│  기존 방식                                                      │
│  ├── 메인 에이전트가 모든 태스크를 상세하게 관리               │
│  ├── 태스크당 약 3,000 토큰 소모                               │
│  └── 50개 태스크 = 150,000+ 토큰 → 컨텍스트 포화!              │
├─────────────────────────────────────────────────────────────────┤
│  Ultra-Thin 방식                                                │
│  ├── 메인 에이전트는 "교통정리"만 담당                         │
│  ├── 서브에이전트가 실제 작업 수행                             │
│  ├── 태스크당 약 190 토큰만 소모                               │
│  └── 200개 태스크 = 38,000 토큰 → 여유!                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 스킬 발동 조건

다음 키워드 중 하나가 감지되면 이 스킬이 발동됩니다:
- `/orchestrate --ultra-thin`
- `/auto-orchestrate --ultra-thin`
- `울트라씬 모드`, `ultra-thin 모드`
- `대규모 태스크 실행`

---

## 실행 단계

### STEP 0: 필수 파일 읽기

스킬 발동 시 **즉시 Read 도구로** 다음 파일들을 읽습니다:

```
1. ~/.claude/skills/ultra-thin-orchestrate/references/orchestrate-rules.md
2. docs/planning/TASKS.md (또는 docs/planning/06-tasks.md)
```

### STEP 1: 상태 파일 초기화/로드

```bash
# 상태 파일 경로
STATE_FILE=".claude/orchestrate-state.json"

# 기존 상태 확인 (--resume 옵션 시)
if [ -f "$STATE_FILE" ]; then
    echo "기존 상태 발견: 재개 가능"
fi
```

### STEP 2: TASKS.md 파싱 및 의존성 분석

**최소 정보만 추출** (Ultra-Thin 핵심):

```
태스크별 추출 정보:
- task_id: T1.1
- phase: 1
- status: pending/in_progress/completed/failed
- depends_on: [T0.5.1, T0.5.2]
- parallel_with: [T1.2]
- specialist: backend-specialist
```

### STEP 3: 실행 루프

```
═══════════════════════════════════════════════════════
🚀 Ultra-Thin Orchestrate 시작!
═══════════════════════════════════════════════════════
📋 프로젝트: {project_name}
📊 총 태스크: {total}개
⚙️ 모드: Ultra-Thin (컨텍스트 절약)

Phase {N} 시작...
├── {task_id} 실행 중...
├── DONE:{task_id}
├── {task_id}, {task_id} 병렬 실행...
├── DONE:{task_id}
├── FAIL:{task_id}:에러메시지
└── Phase {N} 완료! ✅

═══════════════════════════════════════════════════════
🎉 완료! 성공: {success}개 ({percent}%)
       실패: {failed}개
       소요 시간: {duration}
═══════════════════════════════════════════════════════
```

---

## 메인 에이전트 역할 (교통정리만!)

### 허용되는 동작

1. **상태 추적**: 태스크 상태(pending/in_progress/completed/failed) 기록
2. **태스크 디스패치**: Task 도구로 서브에이전트에게 작업 위임
3. **신호 수신**: DONE/FAIL 신호만 수신
4. **다음 태스크 결정**: 의존성 기반으로 다음 실행 가능 태스크 선택

### 금지되는 동작

1. ❌ 태스크 상세 내용 컨텍스트에 보관
2. ❌ 서브에이전트 실행 로그 전체 수신
3. ❌ 에러 상세 내용 컨텍스트에 저장 (파일에만 기록)
4. ❌ 코드 직접 작성/수정

---

## 서브에이전트 프로토콜

### Task 도구 호출 형식

```
Task 도구 호출:
- subagent_type: "general-purpose" 또는 전문가 타입
- prompt: |
    [ULTRA-THIN MODE]
    Task ID: {task_id}
    Phase: {phase}

    작업 지침:
    {task_description}

    완료 시 반드시 다음 형식으로 응답:
    - 성공: "DONE:{task_id}"
    - 실패: "FAIL:{task_id}:{간단한_에러_요약}"

    상세 로그는 .claude/memory/learnings.md에 기록하세요.
```

### 응답 처리

서브에이전트 응답에서 **신호만 추출**:

```
DONE:T1.3          → 상태를 completed로 변경
FAIL:T1.3:TypeError → 상태를 failed로 변경, 재시도 카운터 증가
```

---

## 상태 파일 스키마

```json
{
  "version": "2.0",
  "mode": "ultra-thin",
  "project": "프로젝트명",
  "started_at": "2026-01-25T10:00:00Z",
  "execution": {
    "current_phase": 1,
    "worktree": "worktree/phase-1-auth"
  },
  "tasks": {
    "pending": ["T1.5", "T1.6"],
    "ready": ["T1.3", "T1.4"],
    "in_progress": ["T1.3"],
    "completed": ["T1.1", "T1.2"],
    "failed": []
  },
  "retry_counts": {
    "T1.3": 2
  },
  "stats": {
    "total": 75,
    "completed": 45,
    "failed": 2,
    "success_rate": 0.957
  }
}
```

---

## CLI 옵션

| 옵션 | 설명 | 예시 |
|------|------|------|
| `--ultra-thin` | Ultra-Thin 모드 활성화 | `/auto-orchestrate --ultra-thin` |
| `--phase N` | 특정 Phase만 실행 | `--ultra-thin --phase 2` |
| `--resume` | 중단된 작업 재개 | `--ultra-thin --resume` |
| `--parallel N` | 최대 병렬 실행 수 | `--ultra-thin --parallel 3` |
| `--dry-run` | 실행 계획만 출력 | `--ultra-thin --dry-run` |

---

## 에러 처리 및 자동 재시도

### 재시도 정책

```
최대 재시도: 10회 (기본 3회보다 높음)
재시도 간격: 지수 백오프 (1초, 2초, 4초, ...)
10회 실패 후: 해당 태스크 건너뛰고 계속 진행
```

### 학습 기록

모든 에러와 해결 방법은 자동으로 기록:

```
.claude/memory/learnings.md
```

---

## 완료 후 보고

```
═══════════════════════════════════════════════════════
🎉 Ultra-Thin Orchestrate 완료!
═══════════════════════════════════════════════════════

📊 실행 결과:
   ├── 성공: 73개 (97%)
   ├── 실패: 2개
   │   ├── T3.5: TypeError - 타입 불일치
   │   └── T4.2: 의존성 미설치
   └── 소요 시간: 2시간 15분

💾 상태 파일: .claude/orchestrate-state.json
📝 학습 기록: .claude/memory/learnings.md

다음 명령으로 실패한 태스크만 재실행:
/auto-orchestrate --ultra-thin --resume
═══════════════════════════════════════════════════════
```

---

## 사용 권장 기준

| Task 수 | 권장 모드 | 이유 |
|---------|-----------|------|
| 1-30개 | 일반 모드 | 상세 로그 확인 가능, 디버깅 용이 |
| 30-50개 | RALPH 모드 | 컴팩팅 관리, 학습 전달 |
| **50-200개** | **Ultra-Thin** | 컨텍스트 절약, 연속 실행 |
| 200개+ | Ultra-Thin + Phase 분할 | 안정적인 대규모 처리 |

---

## 토큰 사용량 비교

| 모드 | 50개 태스크 | 100개 태스크 | 200개 태스크 |
|------|------------|-------------|-------------|
| 일반 모드 | ~160K 토큰 | ~320K 토큰 | 불가능 |
| RALPH 모드 | ~80K 토큰 | ~160K 토큰 | ~320K 토큰 |
| **Ultra-Thin** | **~10K 토큰** | **~19K 토큰** | **~38K 토큰** |

---

## Reference 파일

```
~/.claude/skills/ultra-thin-orchestrate/
├── SKILL.md                              # 이 파일
├── references/
│   ├── orchestrate-rules.md              # 상세 실행 규칙
│   └── state-schema.json                 # 상태 파일 스키마
└── scripts/
    ├── parse_tasks.py                    # TASKS.md 파서
    └── dependency_resolver.py            # 의존성 분석기
```
